import SwiftUI

/// Top-level namespace for design tokens.
///
/// All UI code should reach for colors, spacing, radii, motion and type through
/// `Tokens.*` so the look-and-feel can be tuned in one place. The visual brief
/// is "light & airy": warm cream canvas, sage primary, coral accent, generous
/// whitespace, rounded everything, soft shadows.
enum Tokens {}
